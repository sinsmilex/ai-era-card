node_modules/
dist/
.next/
.data/
.env
.env.local
*.tsbuildinfo

.vercel
.env*

# Editor-fetched skill/reference docs (Cursor, etc.) — tool cache, not project source
.agents/

# Agent scratch space — never project source
tmp/
